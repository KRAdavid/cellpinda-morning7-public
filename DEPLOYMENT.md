# GitHub Pages 배포 안내

이 폴더의 파일만 전용 GitHub 저장소 `cellpinda-morning7-public`의 `main` 브랜치 루트에 업로드합니다.

저장소 설정에서 Pages의 Source를 `GitHub Actions`로 선택하면 `.github/workflows/pages.yml`이 자동 배포합니다.

이 공개 사이트에는 내부 회의·원자료·원가·배합·API 키·AI 분석 서버가 포함되어 있지 않습니다.

